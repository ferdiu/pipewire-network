#!/bin/bash
set -e

case "$1" in
    configure)
        echo "PipeWire Network Client installed successfully!"
        echo ""
        echo "Configuration Management:"
        echo "1. Create/edit configurations:"
        echo "   pipewire-network-client-config create-sample"
        echo "   pipewire-network-client-config list"
        echo ""
        echo "2. Enable specific configurations:"
        echo "   pipewire-network-client-config enable default"
        echo "   pipewire-network-client-config enable office"
        echo ""
        echo "3. Using systemd directly:"
        echo "   systemctl --user enable pipewire-network-client@office.service"
        echo "   systemctl --user start pipewire-network-client@office.service"
        echo ""
        echo "4. Check service status:"
        echo "   pipewire-network-client-config status office"
        ;;
esac

#DEBHELPER#
exit 0
